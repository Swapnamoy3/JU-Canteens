<%    layout("/_layouts/boilerpalte_canteens.ejs") %>

<script src="https://cdn.tailwindcss.com"></script>



<main class="  flex justify-center" >
    <ul class=" md:w-[95%] w-[89%] flex mt-20 flex-wrap justify-center">
            <% for(let item of items) {%>
                <li class="m-1 mx-2 rounded-md max-h-80  text-center shadow-md border-gray-100 active:shadow-none hover:opacity-70 border"><a href="/canteens/<%= item.canteen.toLocaleLowerCase() %>/<%= item.type %>/<%= item._id %>">
                    <h3 class="text-center font-serif sm:text-lg"><%= item.item %></h3>
                    <img class="h-64 w-72 mx-auto " src= <%= item.foodImage %> alt="">
                    <p class="font-serif sm:text-md">price: &#8377; <%= item.price %></p>
                    <!-- <p class="font-serif sm:text-md">price: &#8377; <%=// item.canteen %></p>
                    <p class="font-serif sm:text-md">price: &#8377; <%=// item.type %></p>
                </a></li> -->
                <% } %>
    </ul>
</main>

<script src="/javascript/popup.js"></script>